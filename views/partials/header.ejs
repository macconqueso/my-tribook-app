<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Website with Search Bar</title>
</head>
<body>

<header class="header">
    <div class="header__logo">
        <a href="/">Tribook</a>
    </div>
    
    <nav class="header__nav">
        <ul>
            <% if (!isAuthenticated) { %>
                <!-- Show Login and Register buttons when not logged in -->
                <li><a href="/login">Login</a></li>
                <li><a href="/register">Register</a></li>
            <% } else { %>
                <!-- Show personalized greeting and Logout button when logged in -->
                <li>Hello, <%= user.firstName %>!</li>
                <li><a href="/logout">Logout</a></li>
                <% if (isAdmin) { %>
                    <li><a href="/admin/apartment/add-new">Add New Apartment</a></li>
                <% } %>
            <% } %>
        </ul>
    </nav>

    <!-- Optional: Search Bar can be uncommented and used -->
    <!-- 
    <div class="header__button">
        <button class="searchBtn" aria-expanded="false">Search</button>
    </div>
    -->
</header>

<!-- Success and Error Messages outside the header to avoid layout issues -->
<div class="message-container">
    <% if (success_msg && success_msg.length != 0) { %>
        <div class="alert success">
            <%= success_msg %>
        </div>
    <% } %>

    <% if (error_msg && error_msg.length != 0) { %>
        <div class="alert error">
            <%= error_msg %>
        </div>
    <% } %>
</div>

</body>
</html>


